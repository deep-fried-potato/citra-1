import {StyleSheet} from "react-native";

const localStyles =  StyleSheet.create({
    postDetailFooter:{flex:0, flexDirection:'row', backgroundColor:'white'},
    commentBox:{flex:0.9},
    commentInput:{flex:1,margin:6},
    commentButton:{flex:0.1, justifyContent:'center'}
});

export default localStyles;
